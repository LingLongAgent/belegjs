# belegjs — Progress

Neueste oben.

## Done

- **M8 · Download** — Neue Bibliotheksdatei `src/lib/download.ts`: `documentFileName(doc)` baut einen sprechenden, dateisystemsicheren Namen „<Typ>-<Belegnummer>.pdf", ersatzweise „<Typ>-<Titel>.pdf", sonst „<Typ>.pdf" — Umlaute/ß werden transliteriert (ä→ae …), unsichere Zeichen zu `-` kollabiert, Ränder getrimmt (alles pure & getestet). `downloadDocument(doc, env?)` rendert das Dokument via `documentToBlob`, erzeugt eine Object-URL, klickt einen `<a download>` und widerruft die URL wieder; der schmale `DownloadEnv`-Adapter (createElement/createObjectURL/revokeObjectURL) ist injizierbar → vollständig ohne echten Browser testbar. Editor: neue Option `onDownload?(doc)`; oben in der Konfig-Spalte sitzt jetzt ein Primär-Button „📄 PDF herunterladen", der den Callback mit dem aktuellen Dokument aufruft. `main.ts` verdrahtet ihn mit `downloadDocument`. `design.css`: `.editor__download` (blauer Vollbreite-Button). Aus `src/lib` re-exportiert. 7 neue Vitest (110 gesamt) grün, Build ok. (Persistenz folgt M9.)

- **M7 · Übersicht (Dokumentenliste)** — Neue pure Datenebene `src/lib/store.ts`: `DocumentStore` ({documents, activeId}) plus totale, mutationsfreie Funktionen `createStore`/`addDocument`/`openDocument`/`duplicateDocument`/`removeDocument`/`findDocument` und `nextDocumentId` (deterministische, typ-genamespacte IDs `typ-n`, keine Zufälligkeit → stabile Tests) sowie `cloneDocument` (Deep-Copy, keine geteilten Referenzen). Löschen verschiebt die aktive Auswahl auf den Vorgänger bzw. null. UI: `src/app/overview.ts` — `createOverview({onCreate,onOpen,onDuplicate,onDelete})` rendert die linke Sidebar aus dem Store: Header mit „+ Angebot/Rechnung/Mahnung", Zeilen mit farbigem Typ-Badge, Titel + Nummer (Fallback „ohne Nummer"), Hervorhebung des aktiven Dokuments, Duplizieren-/Löschen-Icons; Empty-State wenn leer. Komponente hält keine Daten — alle Aktionen sind Callbacks, der Host (main.ts) bleibt Single Source of Truth. `main.ts` neu als App-Shell: Store + Overview links, 3-Spalten-Editor rechts; Öffnen lädt das aktive Dokument via `editor.setDocument`, Editor-`onChange` synchronisiert die Store-Zeile (Titel/Nummer live in der Liste), kein offenes Dokument → Editor ausgeblendet. `design.css`: `.shell` (flex), `.overview`-Sidebar (280px), `.doc-row`, `.badge--angebot/rechnung/mahnung`, Icon-Buttons; responsive < 980px stapelt Sidebar über Editor. 27 neue Vitest (103 gesamt) grün, Build ok. (Download folgt M8.)

- **M6 · 3-Spalten-Editor** — `src/app/editor.ts`: `createEditor({document, onChange?, previewFactory?})` baut den vollständigen Editor — links editierbarer Inhalt, Mitte die Live-Vorschau (M5), rechts das Config-Panel. Eine einzige mutierbare `BelegDocument`-Instanz ist Single Source of Truth; jeder Input mutiert sie und ruft `emit()` (→ `preview.update(doc)` + `onChange`). Texteingaben bauen das DOM **nicht** neu (Fokus/Cursor bleiben erhalten); nur strukturelle Änderungen (Position hinzufügen/entfernen, Dokumenttyp wechseln) rendern ihren Abschnitt neu. Editierbar: Typ, Titel, Nummer/Datum/Ihr-Zeichen, typabhängige Felder (Gültig bis / Fällig am / Bezugsrechnung+Mahnstufe+Mahngebühr), Empfänger & Absender (Firma/Name/Straße/PLZ/Ort), Einleitungs-/Schlusstext sowie Positionen (Beschreibung/Menge/Einzelpreis €/USt %) mit Hinzufügen/Entfernen. Config: Schriftart, Ausrichtung, DIN-5008-Form, Footer, Seitenzahl, Kleinunternehmer §19. €↔Cent-Umrechnung (`euroToCents`/`centsToEuro`). Preview ist injizierbar → DOM-Tests ohne echtes PDF/jsPDF. `main.ts` mountet den Editor auf einer Demo-Rechnung; `design.css` neu: Topbar + CSS-Grid (340px·1fr·320px, unter 980px einspaltig), Felder/Buttons/Positionskarten. Hinweis: `tsconfig` ohne `DOM.Iterable` → in Tests `Array.from(...)` statt Spread über NodeLists. 11 neue Vitest (76 gesamt) grün, Build ok. (Dokumentenliste/Übersicht folgt M7.)
- **M5 · Live-Vorschau** — `src/app/preview.ts`: `createPreview()` liefert einen schlanken DOM-Controller (`element`/`update(doc)`/`destroy()`) für die mittlere Spalte. `update` rendert das Dokument via `documentToBlob` in ein PDF-Blob, hängt es als Object-URL an ein `<iframe>` (PDF-Viewer-Chrome via `#toolbar=0&navpanes=0&view=FitH` ausgeblendet) und widerruft die vorherige URL → kein Leak bei jedem Tastendruck. Das „Blatt" hält die A4-Proportion (`A4_ASPECT_RATIO` = 210/297, pure & getestet) per CSS, skaliert also als Papierbogen. `main.ts` zeigt jetzt eine echte Demo-Rechnung in der Live-Vorschau; CSS für `.preview`/`.preview__sheet`/`.preview__pdf`. happy-dom als Dev-Dep für DOM-Tests (`// @vitest-environment happy-dom`). 6 neue Vitest (65 gesamt) grün, Build ok. (3-Spalten-Editor folgt M6.)
- **M4 · Dokumenttypen** — `documents.ts`: reine, getestete typabhängige Inhalte. `buildPositionTable` (Pos./Beschreibung/Menge/Einzelpreis/USt./Gesamt; USt.-Spalte entfällt bei Kleinunternehmer), `buildSummary` (Netto, USt je Satz, fetter Gesamtbetrag; §19-Hinweis; Mahnung: Offener Betrag + Mahngebühr + fette Gesamtforderung), `typeSpecificNote` (Angebot „gültig bis", Rechnung Zahlungsziel, Mahnung-Absatz mit Bezug/Stufe/Forderung/Frist), `buildDocumentContent` rechnet Totals einmal und hält Tabelle/Summe/Notiz konsistent. `pdf.ts` zeichnet jetzt Positionstabelle (Spaltenbreiten, Beschreibungsumbruch), rechtsbündigen Summenblock (fette Beträge, §19-Hinweis) und die Notiz zwischen Intro und Outro. 19 neue Vitest (59 gesamt) grün, Build ok.
- **M3 · PDF-Renderer (jsPDF)** — `layout.ts`: reine, getestete DIN-5008-Textbausteine (`formatDateDE`, `recipientLines`, `senderReturnLine`, `infoBlockRows`, `subjectLine`) — kennt die Anschrift-/Infoblock-Regeln, typabhängige Zeilen (Gültig bis/Fällig am/Mahnstufe), ohne jsPDF. `pdf.ts`: `renderLetter` zeichnet Faltmarken, Anschriftfeld (Rücksendezeile + Empfänger), rechtsbündigen Infoblock, fetten Betreff, umbrochenen Fließtext (Ausrichtung links/Blocksatz), Footer + Seitenzahl — alles aus DocConfig (Schrift, Form A/B). `documentToBlob`/`documentToDataUrl` für Download/Vorschau. 18 neue Vitest (40 gesamt) grün, Build ok. (Positionstabelle folgt in M4.)
- **M2 · USt & Formatierung** — `money.ts`: Cent-Mathematik, `computeTotals` (USt je Satz gruppiert, Gruppen-Rundung, aufsteigend sortiert), Kleinunternehmer §19 (keine USt, Gross=Net, Hinweis), deutsche Formatierung (`formatEuro/Number/Quantity/Percent` via Intl de-DE). 12 neue Vitest (22 gesamt) grün, Build ok.
- **M1 · Dokumentmodell + Config** — Typen Angebot/Rechnung/Mahnung (Address, Position in Cent, DocMeta) + DocConfig (Schrift, Footer, Seitenzahl, Ausrichtung, Kleinunternehmer, Form). `createDocument`/`isDocType` mit Defaults & Deep-Copy. 10 Vitest grün, Build ok.
- **M0 · Scaffold** — Vite+TS + DIN-Geometrie (Form A/B) + Editor-Gerüst. 4 Vitest grün, Build ok.
